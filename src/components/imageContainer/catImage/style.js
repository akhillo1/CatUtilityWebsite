export const styles = (theme) => ({
    root: {
        flexGrow: 1,
      },
      img:{
        marginTop: '-50px',
        width: '100%',
      },
      favIcon: {
          float: 'right'
      }
});
